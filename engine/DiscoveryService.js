

module.exports = class DiscoveryService {

    constructor(options) {
        super(options);
        this._uri = options.uri;
        this._connection = null;
    }

    getServer(stage, stage, key) {
    }

    addStream(stage, stage, key, serverName) {
    }

    registerServer(serverName) {
    }

    removeServer(serverName) {
    }
}
